let valores = [8, 3, 7, 4, 2, 9]

for(let pos=0; pos<valores.length; pos++) {
    console.log(`A posição ${pos} tem o valor ${valores[pos]}`)
}

/*
for(let pos in valores) {
    console.log(`A posição ${pos} tem o valor ${valores[pos]}`)
}
*/

/*
num.indexOf(7)
//mostra qual posição da lsita está o valor que foi pedido
//caso retorna o valor -1 é porque o valor que foi passado não está na lsita
*/